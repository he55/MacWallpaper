<Window x:Class="MacWallpaper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MacWallpaper"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="640"
        Loaded="Window_Loaded"
         xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True">
    <Window.Resources>
        <Style x:Key="GridViewItemStyle" TargetType="ui:GridViewItem">
            <Setter Property="VerticalContentAlignment" Value="Top"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="500"/>
        </Grid.ColumnDefinitions>
        <ListBox x:Name="listBox" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding str1}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid DataContext="{Binding SelectedItem, ElementName=gridView}" Margin="20,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Border CornerRadius="4" Width="107" Height="65">
                    <Border.Background>
                        <ImageBrush ImageSource="{Binding previewImage}"/>
                    </Border.Background>
                </Border>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="{Binding str1}" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="显示为屏幕保护程序"/>
                        <ui:ToggleSwitch OnContent="" OffContent="" IsOn="True"/>
                    </StackPanel>
                    <ui:ProgressBar Value="{Binding progress}"/>
                    <Button Content="下载" Click="Button_Click_1"/>
                </StackPanel>
            </Grid>
            <ui:GridView x:Name="gridView" ItemContainerStyle="{StaticResource GridViewItemStyle}" 
                     HorizontalAlignment="Left" Width="500" 
                     ItemsSource="{Binding SelectedItem.assets, ElementName=listBox}" Grid.Row="1">
                <ui:GridView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Width="107" Margin="5">
                            <Border CornerRadius="4" Width="107" Height="65">
                                <Border.Background>
                                    <ImageBrush ImageSource="{Binding previewImage}"/>
                                </Border.Background>
                            </Border>
                            <TextBlock Text="{Binding str1}" TextWrapping="Wrap" TextAlignment="Center" FontSize="10" FontWeight="Bold" Margin="0,2,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </ui:GridView.ItemTemplate>
            </ui:GridView>

        </Grid>


    </Grid>
</Window>
